<ui:FluentWindow x:Class="WpfApp1.PipWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="PiP" Height="540" Width="440"
        MinWidth="280" MinHeight="300"
        WindowStyle="ToolWindow" ResizeMode="CanResizeWithGrip"
        Topmost="True" ShowInTaskbar="False" WindowStartupLocation="Manual"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="PipDrag" Grid.Row="0"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                MouseLeftButtonDown="PipDrag_MouseLeftButtonDown" Cursor="SizeAll">
            <Border.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="PipCloseItem" Click="PipCloseItem_Click"/>
                </ContextMenu>
            </Border.ContextMenu>
            <TextBlock x:Name="PipTitle" VerticalAlignment="Center" Margin="12,0"
                       FontSize="12" Opacity="0.8"/>
        </Border>

        <wv2:WebView2 x:Name="PipWeb" Grid.Row="1"/>
    </Grid>
</ui:FluentWindow>
