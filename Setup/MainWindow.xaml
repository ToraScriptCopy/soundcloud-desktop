<ui:FluentWindow x:Class="Setup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Setup" Height="420" Width="520"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="SetupBar" Grid.Row="0" Title="Setup"/>

        <Grid Grid.Row="1" Margin="20">
            <!-- Page 1: language -->
            <StackPanel x:Name="PageLang">
                <TextBlock Text="SoundCloud Desktop Beta" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="LangLbl" Margin="0,16,0,6"/>
                <ComboBox x:Name="LangBox" SelectionChanged="LangBox_SelectionChanged"/>
                <ui:Button x:Name="BtnNext1" Appearance="Primary" HorizontalAlignment="Right"
                           Margin="0,24,0,0" MinWidth="120" Click="BtnNext1_Click"/>
            </StackPanel>

            <!-- Page 2: folder + options -->
            <StackPanel x:Name="PageMain" Visibility="Collapsed">
                <TextBlock x:Name="PathLbl" Margin="0,0,0,6"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="PathBox" Grid.Column="0" IsReadOnly="True"/>
                    <ui:Button x:Name="BtnBrowse" Grid.Column="1" Margin="6,0,0,0" Click="BtnBrowse_Click"/>
                </Grid>
                <CheckBox x:Name="OptDesktop" Margin="0,14,0,0"/>
                <CheckBox x:Name="OptStartMenu" Margin="0,6,0,0"/>
                <CheckBox x:Name="OptLaunch" Margin="0,6,0,0"/>
                <TextBlock x:Name="WebViewWarn" Margin="0,12,0,0" TextWrapping="Wrap"
                           Foreground="Orange" Visibility="Collapsed"/>
                <ui:Button x:Name="BtnWebView" Margin="0,6,0,0" Visibility="Collapsed"
                           HorizontalAlignment="Left" Click="BtnWebView_Click"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <ui:Button x:Name="BtnBack" Margin="0,0,8,0" MinWidth="110" Click="BtnBack_Click"/>
                    <ui:Button x:Name="BtnInstall" Appearance="Primary" MinWidth="130" Click="BtnInstall_Click"/>
                </StackPanel>
            </StackPanel>

            <!-- Page 3: progress -->
            <StackPanel x:Name="PageProgress" Visibility="Collapsed" VerticalAlignment="Center">
                <TextBlock x:Name="ProgStatus" Margin="0,0,0,10"/>
                <ProgressBar x:Name="ProgBar" Height="10" Minimum="0" Maximum="100"/>
            </StackPanel>

            <!-- Page 4: done -->
            <StackPanel x:Name="PageDone" Visibility="Collapsed" VerticalAlignment="Center">
                <TextBlock x:Name="DoneText" FontSize="16" TextWrapping="Wrap"/>
                <ui:Button x:Name="BtnClose" Appearance="Primary" HorizontalAlignment="Right"
                           Margin="0,24,0,0" MinWidth="120" Click="BtnClose_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
