<ui:FluentWindow x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="SoundCloud Desktop Alt" Height="760" Width="1180"
        MinHeight="560" MinWidth="860"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        StateChanged="MainWindow_StateChanged"
        Closing="MainWindow_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="SoundCloud Desktop Alt" Height="30"/>

        <Grid Grid.Row="1" Margin="8,0,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <ui:Button x:Name="BtnMenu" Margin="0,0,4,0" Padding="8,5" Click="BtnMenu_Click">
                    <Path Data="{StaticResource LucideMenu}" Style="{StaticResource LucideIcon}" Width="15" Height="15"/>
                </ui:Button>
                <ui:Button x:Name="BtnBack" Margin="0,0,4,0" Padding="8,5" Click="BtnBack_Click">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowLeft24"/></ui:Button.Icon>
                </ui:Button>
                <ui:Button x:Name="BtnFwd" Margin="0,0,4,0" Padding="8,5" Click="BtnFwd_Click">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowRight24"/></ui:Button.Icon>
                </ui:Button>
                <ui:Button x:Name="BtnReload" Margin="0,0,4,0" Padding="8,5" Click="BtnReload_Click">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="ArrowClockwise24"/></ui:Button.Icon>
                </ui:Button>
                <ui:Button x:Name="BtnHome" Padding="8,5" Click="BtnHome_Click">
                    <ui:Button.Icon><ui:SymbolIcon Symbol="Home24"/></ui:Button.Icon>
                </ui:Button>
            </StackPanel>
            <ui:TextBox x:Name="AddrBox" Grid.Column="1" Margin="6,0" Padding="8,5"
                        KeyDown="AddrBox_KeyDown"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <ui:Button x:Name="BtnGo" Appearance="Primary" Padding="10,5" Click="BtnGo_Click"/>
                <ui:Button x:Name="BtnSettings" Margin="4,0,0,0" Padding="8,5" Click="BtnSettings_Click">
                    <Path Data="{StaticResource LucideSettings}" Style="{StaticResource LucideIcon}" Width="15" Height="15"/>
                </ui:Button>
            </StackPanel>
        </Grid>

        <Grid x:Name="MainContent" Grid.Row="2" Margin="8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarCol" Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer x:Name="SidebarView" Grid.Column="0" Width="210" VerticalScrollBarVisibility="Auto" Margin="0,0,8,0">
                <StackPanel>
                    <ui:Button x:Name="BtnNavHome" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Padding="8,6" Click="BtnHome_Click">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource LucideHouse}" Style="{StaticResource LucideIcon}" Width="15" Height="15"/>
                            <TextBlock x:Name="NavHomeTxt" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                    <ui:Button x:Name="BtnNavCharts" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,4" Padding="8,6" Click="BtnCharts_Click">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource LucideChart}" Style="{StaticResource LucideIcon}" Width="15" Height="15"/>
                            <TextBlock x:Name="NavChartsTxt" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                    <ui:Button x:Name="BtnNavLikes" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Margin="0,0,0,12" Padding="8,6" Click="BtnLikes_Click">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource LucideHeart}" Style="{StaticResource LucideIcon}" Width="15" Height="15"/>
                            <TextBlock x:Name="NavLikesTxt" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>

                    <TextBlock x:Name="PipHeader" Margin="2,0,0,6" FontSize="11" Opacity="0.6" FontWeight="SemiBold"/>
                    <ui:TextBox x:Name="PlaylistBox" Padding="8,5" KeyDown="PlaylistBox_KeyDown"/>
                    <ui:Button x:Name="BtnPipPlay" Appearance="Primary" HorizontalAlignment="Stretch"
                               Margin="0,6,0,0" Padding="8,6" Click="BtnPipPlay_Click"/>
                    <ui:Button x:Name="BtnPipOpen" HorizontalAlignment="Stretch"
                               Margin="0,4,0,0" Padding="8,6" Click="BtnPipOpen_Click"/>
                    <TextBlock x:Name="PipHint" Margin="2,6,0,12" FontSize="11" Opacity="0.6" TextWrapping="Wrap"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <ui:Button x:Name="BtnCopy" Padding="8,5" Click="BtnCopy_Click">
                            <Path Data="{StaticResource LucideCopy}" Style="{StaticResource LucideIcon}" Width="14" Height="14"/>
                        </ui:Button>
                        <ui:Button x:Name="BtnExt" Margin="4,0,0,0" Padding="8,5" Click="BtnExt_Click">
                            <Path Data="{StaticResource LucideExternal}" Style="{StaticResource LucideIcon}" Width="14" Height="14"/>
                        </ui:Button>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <wv2:WebView2 x:Name="Browser" Grid.Column="1"/>
        </Grid>

        <Border Grid.Row="3" Margin="8,3,8,5" Padding="7,4" CornerRadius="12"
                Background="{StaticResource RadixGray2Brush}" BorderBrush="{StaticResource RadixGray4Brush}" BorderThickness="1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                    <ui:Button x:Name="BtnPipMini" Padding="7,4" Margin="0,0,6,0" Click="BtnPipMini_Click">
                        <Path Data="{StaticResource LucidePip}" Style="{StaticResource LucideIcon}" Width="14" Height="14"/>
                    </ui:Button>
                    <Path x:Name="VolIcon" Data="{StaticResource LucideVol}" Width="15" Height="15"
                          Stretch="Uniform" Fill="Transparent" StrokeThickness="2"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                          Stroke="{DynamicResource TextFillColorPrimaryBrush}"
                          VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <Slider x:Name="VolSlider" Width="110" Minimum="0" Maximum="100" Value="80"
                            VerticalAlignment="Center" ValueChanged="VolSlider_ValueChanged"/>
                    <TextBlock x:Name="VolLabel" Text="80%" Width="36" VerticalAlignment="Center" Margin="6,0" FontSize="12"/>
                    <ui:ToggleSwitch x:Name="MuteSwitch" FontSize="12"
                                     Checked="MuteSwitch_Changed" Unchecked="MuteSwitch_Changed"/>
                </StackPanel>
                <TextBlock x:Name="NowPlaying" VerticalAlignment="Center" FontSize="12"
                           TextTrimming="CharacterEllipsis" Margin="0,0,10,0"/>
            </DockPanel>
        </Border>

        <ui:SnackbarPresenter x:Name="SnackbarPresenter"
                              Grid.Row="0" Grid.RowSpan="4"
                              VerticalAlignment="Bottom" HorizontalAlignment="Center"
                              Margin="0,0,0,60"/>
        <ui:ContentDialogHost x:Name="DialogHost"
                              Grid.Row="0" Grid.RowSpan="4"/>
    </Grid>
</ui:FluentWindow>
